import React, { Component } from "react";
import { View, Text, ScrollView } from "react-native";
import { connect } from "react-redux";

class FilterPreview extends Component {
  render() {
    return (
      <View>
        <Text> Filter Preview </Text>
      </View>
    );
  }
}

export default FilterPreview;
